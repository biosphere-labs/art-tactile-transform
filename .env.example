# Art Tactile Transform - Configuration Defaults
# Copy this file to .env and customize as needed

# AI Model for depth estimation mode (CLI only)
MODEL_NAME=LiheYoung/depth-anything-small-hf

# Processing Resolution (vertices per side)
RESOLUTION=64

# Physical Dimensions (millimeters)
MIN_HEIGHT_MM=0.2
MAX_HEIGHT_MM=2.0
BASE_THICKNESS_MM=1.0
PIXEL_SCALE_MM=0.2

# Processing Options
GAUSSIAN_BLUR_RADIUS=0
CLAMP_MIN=0
CLAMP_MAX=255
BORDER_PIXELS=0
INVERT_HEIGHTS=false

# Optional: Hugging Face API token (for model downloads)
HF_API_TOKEN=

# Note: IMAGE_PATH and OUTPUT_PATH are not needed
# The GUI handles file upload/download dynamically
